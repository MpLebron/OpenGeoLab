# Copy this file to .env.development or .env.production and adjust the values.
# Development usually talks to the Express server on localhost:3000.
# Production should use the public /OpenGeoLab/ prefix without exposing backend ports.

VITE_PUBLIC_BASE_PATH=/
VITE_API_BASE_URL=http://localhost:3000
VITE_MAPMYVISITORS_WIDGET_ID=
