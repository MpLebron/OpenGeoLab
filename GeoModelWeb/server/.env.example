# Copy this file to .env.development or .env.production and adjust the values.
# Never commit real .env files.

GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

JWT_SECRET=replace-with-a-random-local-secret

PORT=3000
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3000
PUBLIC_ORIGIN=http://localhost:3000

# Jupyter gateway. In production, public notebook URLs use PUBLIC_ORIGIN + /jupyter/<workspaceId>/.
JUPYTER_PUBLIC_BASE_PATH=/jupyter
JUPYTER_BIND_HOST=127.0.0.1
JUPYTER_OPENGEOLAB_API=http://host.docker.internal:3000/api

USER_DATA_DIR=./jupyter-data

MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB_NAME=opengms_jupyter

OPENGMS_USER_SERVER_URL=
OPENGMS_CLIENT_ID=
OPENGMS_CLIENT_SECRET=
OPENGMS_AVATAR_BASE_URL=
OPENGMS_PASSWORD_ENCODING=sha256_md5

OGMS_TOKEN=replace-with-your-opengms-token
API_TOKEN=replace-with-your-opengms-token
OGMS_DEPLOYED_MODEL_QUERY_TOKEN=replace-with-your-deployed-model-query-token

# Notebook live model execution. By default, notebooks submit live OpenGMS tasks and
# wait for the SDK result. Set OPENGMS_LIVE_MODE=archive only for offline rendering.
OPENGMS_LIVE_MODE=submit
OPENGMS_LIVE_TIMEOUT_SECONDS=0
